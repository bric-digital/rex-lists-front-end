/**
 * Browser context module for webmunk-block-allow
 *
 * This module runs in the browser context (injected into web pages).
 * Currently minimal as list management happens in extension/service worker contexts.
 */

console.log('[webmunk-block-allow] Browser module loaded')

// Currently no browser-side functionality needed
// List management is handled by service worker and extension UI
